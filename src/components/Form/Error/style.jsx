import styled from 'styled-components';

export const ErrorContent = styled.span`
  background: #ee585a;
  border-radius: 4px;
  padding: 0.4rem;
  color: #fff;
  margin: -0.2rem 0.6rem;
  display: grid;
`;
